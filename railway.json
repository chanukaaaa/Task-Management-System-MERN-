{
    "build": {
      "builder": "Nixpacks",
      "plan": {
        "frontend": {
          "start": "cd frontend && npm install && npm run dev"
        },
        "backend": {
          "start": "cd backend && npm install && npm run server"
        }
      }
    }
  }
  